/*
Assignment 04
Numbers are strictly prohibited; you may only use the number 0.
Only one line of code must be written to generate the required value.
*/

let website = "Go";

let words = [`${website}ogle`, "Facebook", ["Elzero", "Web", "School"]];

console.log(words[website.length][0].slice(website.length).toUpperCase()); // ZERO
