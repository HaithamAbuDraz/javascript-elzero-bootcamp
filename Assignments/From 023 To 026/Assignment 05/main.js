/*
Assignment 05
The following variable contains the number 10.
You must use `isInteger` to get the result 2.
You are not allowed to enter any numbers yourself; you must use the previous property to get the required result.

*/

let num = 10;

// Using Number.isInteger() returns true (1), then add two trues to get 2
console.log(Number.isInteger(num) + Number.isInteger(num)); // 2
